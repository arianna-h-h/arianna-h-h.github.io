import React from 'react';

/* TODO write function to fetch image from google images API using the dog's
description and display image here */
export const DogImage = ({ dog }) => {
  return (
    <div className='question-mark'>
      <i className='fas fa-question fa-10x' />
    </div>
  );
};
